import { useState } from 'react';
import type { ReportType } from '../types/api';
export default function ReportInput({onSubmit,loading}:{onSubmit:(v:{text:string;type:ReportType;site:string|null;date:string|null})=>void;loading?:boolean}){const [text,setText]=useState('');const [type,setType]=useState<ReportType>('Near Miss');const [site,setSite]=useState('');const [date,setDate]=useState('');return <div className="input-panel"><label>Safety report<textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Paste the workplace safety report here…" rows={9}/></label><div className="form-grid"><label>Report type<select value={type||''} onChange={e=>setType((e.target.value||null) as ReportType)}><option value="Near Miss">Near Miss</option><option value="UA">UA</option><option value="UC">UC</option><option value="Incident">Incident</option><option value="">Not specified</option></select></label><label>Site<input value={site} onChange={e=>setSite(e.target.value)} placeholder="Optional"/></label><label>Date<input type="date" value={date} onChange={e=>setDate(e.target.value)} /></label></div><button className="button primary" disabled={!text.trim()||loading} onClick={()=>onSubmit({text,type,site:site||null,date:date||null})}>{loading?'Analyzing…':'Analyze report'}</button></div>}
