import type { ReportsListQuery } from '../types/api';
export default function FilterBar({value,onChange}:{value:ReportsListQuery;onChange:(v:ReportsListQuery)=>void}){return <div className="filters"><input placeholder="Search report, site or activity…" value={value.search||''} onChange={e=>onChange({...value,search:e.target.value,page:1})}/><select value={value.sif||'all'} onChange={e=>onChange({...value,sif:e.target.value as ReportsListQuery['sif'],page:1})}><option value="all">All SIF status</option><option value="sif">SIF potential</option><option value="non-sif">Non-SIF</option></select><input placeholder="Site" value={value.site||''} onChange={e=>onChange({...value,site:e.target.value||undefined,page:1})}/><input placeholder="Activity" value={value.activity||''} onChange={e=>onChange({...value,activity:e.target.value||undefined,page:1})}/><input placeholder="Life-Saving Rule" value={value.rule||''} onChange={e=>onChange({...value,rule:e.target.value||undefined,page:1})}/></div>}
