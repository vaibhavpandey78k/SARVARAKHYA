import { useEffect, useState } from 'react'; import { api } from '../api'; import type { Precursor } from '../types/api'; import { EmptyState, ErrorState, LoadingState, Section } from '../components/ui'; import { PrecursorTable } from '../components/Tables';
export default function Precursors(){const [items,setItems]=useState<Precursor[]|null>(null);const [error,setError]=useState('');useEffect(()=>{api.getDashboardPrecursors().then(x=>setItems(x.items)).catch(e=>setError(e.message||'Precursor data unavailable'));},[]);if(error)return <ErrorState title="Precursor explorer unavailable" text={error}/>;if(!items)return <LoadingState/>;return <><div className="page-intro"><div><span className="eyebrow">PATTERN INTELLIGENCE</span><h2>Precursor Explorer</h2><p>Recurring Activity + Location + Barrier Failure combinations returned by the backend.</p></div></div><Section title="Recurring precursor patterns"><p className="section-note">Occurrence, SIF count, density and trend are displayed exactly as returned. The frontend does not recompute business metrics.</p>{items.length?<PrecursorTable items={items}/>:<EmptyState/>}</Section></>}
